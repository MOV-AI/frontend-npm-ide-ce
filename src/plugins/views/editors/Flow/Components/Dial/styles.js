import { makeStyles } from "@material-ui/core/styles";

export const actionDial = makeStyles(theme => ({
    base: {
        position: "absolute",
        bottom: "20px",
        right: "20px"
    },
}));